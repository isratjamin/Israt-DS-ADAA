Count the total number of even and odd elements present in an array.


......code......

#include <stdio.h>

int main() {
    int arr[] = {2, 5, 6, 7, 10};  // Array fixed
    int i, even = 0, odd = 0;

    for(i = 0; i < 5; i++) {
        if(arr[i] % 2 == 0)
            even++;
        else
            odd++;
    }

    printf("Even: %d\n", even);
    printf("Odd: %d\n", odd);

    return 0;
}


output:
even:3
odd:2
