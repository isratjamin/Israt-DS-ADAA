 Implement the linear search algorithm in C

......code.......

#include <stdio.h>

int main() {
    int n, i, key, a[100];
    scanf("%d", &n);
    for(i=0; i<n; i++) scanf("%d", &a[i]);
    scanf("%d", &key);

    for(i=0; i<n && a[i]!=key; i++);
    if(i<n) printf("Found at position %d\n", i+1);
    else printf("Not found\n");

    return 0;
}

INPUT:
5
10 20 30 40 50
30

OUTPUT:
Found at position 3
