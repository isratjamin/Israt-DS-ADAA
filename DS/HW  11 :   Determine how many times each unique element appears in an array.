 Determine how many times each unique element appears in an array.


.....code.....

#include <stdio.h>

int main() {
    int arr[] = {2, 3, 2, 5, 3, 2, 5, 7};
    int n = sizeof(arr) / sizeof(arr[0]);
    int freq[100] = {0};  

    // frequency count
    for (int i = 0; i < n; i++) {
        freq[arr[i]]++;
    }

    // print result
    for (int i = 0; i < 100; i++) {
        if (freq[i] > 0) {
            printf("%d occurs %d times\n", i, freq[i]);
        }
    }

    return 0;
}

Output:
2 occurs 3 times
3 occurs 2 times
5 occurs 2 times
7 occurs 1 times
